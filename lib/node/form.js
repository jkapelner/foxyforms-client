//there is no form integration with node, so do nothing (this is just a placeholder so we stay compatible with the browser version)

exports.parse = function(fields, verifyController) { 
  return fields;
};

exports.init = function(forms, verifyController) {
};
